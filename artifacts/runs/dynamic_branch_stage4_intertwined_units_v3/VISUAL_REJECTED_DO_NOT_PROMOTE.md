# Visual rejection

This package passed numeric diagnostics but failed the human visual gate. The
units read as cluttered local curve clusters and did not retain the fixed
version's visual organization. It must not be promoted or used as a planning
input.

The replacement route restarts composition planning at:

`artifacts/runs/dynamic_branch_stage3b_global_l1_flow_v1`

The original files remain unchanged as failure evidence.
